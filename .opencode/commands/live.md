---
description: Visual variant mode — pick elements, generate alternatives
---

Interactive live variant mode. Load the `impeccable` skill, select elements in the browser, pick a design action, and get AI-generated HTML+CSS variants hot-swapped via HMR. Requires a running dev server.
